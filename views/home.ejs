<%- include("partials/header")%>
<h1>Home</h1>
<p><%= homeContent %></p>
<% posts.forEach((post)=>{%>
    <h2><%= post.title %></h2>
    <%if(post.content.lenght>100){%>
    <p><%= post.content.slice(0,100) %></p>
    <%}else{%>
        <p><%= post.content%></p>
    <%}%>
    <%})%>
<%- include("partials/footer")%>
